/// <summary>
/// Enum similar to PonyAge, contains an Any value.
/// Used for specifying the required age for using an item.
/// </summary>
public enum RequiredAge {
    Any = 0,
    Adult = 1,
    Foal = 2
}