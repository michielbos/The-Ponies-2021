pipeline {
    agent any
    stages {
        stage ('Init') {
            steps {
                sh 'echo Init placeholder.'
            }
        }
        stage ('Build') {
            steps {
                sh 'ls'
            }
        }
        stage('Archive') {
            steps {
                sh 'echo Archive placeholder.'
            }
        }
    }
}
