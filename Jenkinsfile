pipeline {
    agent any
    stages {
        stage ('Init') {
            steps {
                sh 'echo Init placeholder.'
            }
        }
        stage ('Build') {
            steps {
                sh 'echo Build placeholder.'
            }
        }
        stage('Archive') {
            steps {
                sh 'echo Archive placeholder.'
            }
        }
    }
}
